#include <stdio.h>

int main(void){
    float x;
    float y;
    // Prompt user for x
    printf("x: ");
    scanf("%f",&x);
    // Prompt user for y
    printf("y: ");
    scanf("%f", &y);
    // Perform addition
    printf("%f", x + y);
     
}